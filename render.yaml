services:
  - type: web
    name: exceldraw-api
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build --filter http-backend
    startCommand: pnpm run start --filter http-backend
    envVars:
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: exceldraw-ws
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build --filter websockets
    startCommand: pnpm run start --filter websockets
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
